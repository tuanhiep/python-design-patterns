[project]
name = "python-design-patterns"
version = "0.1.0"
description = "Python examples for design pattern practice."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
